apiVersion: v1
kind: Service
metadata:
  name: kibana-bridge
  namespace: default
spec:
  type: ExternalName
  externalName: searchx-kb-http.elasticsearch.svc.cluster.local
  ports:
    - port: 5601
      targetPort: 5601
