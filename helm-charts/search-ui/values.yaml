replicaCount: 1

image:
  repository: search-ui
  tag: 7003679a
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "128Mi"
    cpu: "200m"

probes:
  readiness:
    initialDelaySeconds: 5
    periodSeconds: 10
